<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  
    <%= f.text_area :content, :rows=> 3 %>
  
  <div class="actions">
    <%= f.submit "Submit", :class=>"btn big" %>
  </div>
<% end %>